<?xml version="1.0" encoding="UTF-8"?>

<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
  <plugin-info>
    <description>${project.description}</description>
    <version>${project.version}</version>
    <vendor name="${project.organization.name}" url="${project.organization.url}"/>
    <param name="plugin-icon">images/pluginIcon.png</param>
    <param name="plugin-logo">images/pluginLogo.png</param>
  </plugin-info>

  <!-- I18N Resources -->
  <resource type="i18n" name="i18n" location="oauth"/>

  <!-- Web Resources -->
  <web-resource key="oauth-resources" name="oauth Web Resources">
    <dependency>com.atlassian.auiplugin:ajs</dependency>
    <resource type="download" name="oauth.css" location="/css/oauth.css"/>
    <resource type="download" name="oauth.js" location="/js/oauth.js"/>
    <resource type="download" name="images/" location="/images"/>
    <context>oauth</context>
  </web-resource>

	<!-- Configuration menu -->
  <web-item name="OAuth Login Config" i18n-name-key="config.name" key="config" section="system.admin/security" weight="1000">
    <description key="config.description">OAuth Login Config</description>
    <label key="config.label"></label>
    <link linkId="config-link">/plugins/servlet/oauth/config.action</link>
	</web-item>

</atlassian-plugin>
